# 项目配置

## 全局硬性规则

- 除专业术语外，所有内容使用**中文**回复。
- 即使加载的 skill、agent、command、hook 输出或示例是英文，也必须用中文向用户提问、解释和总结。
- 英文内容仅作为执行流程或技术材料参考，不改变用户交流语言。

## 用户沟通

- 面向用户先说结论，再说明原因、影响和下一步；不要先堆日志、内部实现或术语。
- 默认使用日常中文。必须使用专业术语、错误码或英文缩写时，紧跟一句解释它对用户意味着什么。
- 解释变更、故障或排查结果时，简单说明；明确区分已确认事实、推测和阻塞项。
- 除非用户要求，不贴大段原始日志、命令或实现细节；只保留支持结论所需的最小证据。

> 规则按需读取，不要默认全量加载 `rules/` 或 `rules/common/`。

## 规则加载策略

- 默认不全量读取 `rules/`。涉及代码修改、审查、测试、提交或 Harness 调整时，按下表“何时读取”只加载相关规则文件。
- 规则路径解析顺序：先检查当前项目根目录 `rules/`；项目无 `rules/` 或目标文件不存在时，回退到用户级规则目录（Codex 用 `~/.codex/rules/`，Claude Code 用 `~/.claude/rules/`），不能把项目规则目录缺失等同于“无规则”；回退只改变查找位置，不改变按需读取原则。
- `rules/common/` 是专项参考区；只有命令、agent、skill 或当前任务明确触发时才读取。

## 规则索引

| 规则文件 | 何时读取 | 内容 |
| --- | --- | --- |
| `01-base.md` | 任何代码实现、非平凡任务开始前 | 执行原则、最小实现决策阶梯、流程边界 |
| `02-code-size.md` | 编辑代码文件后 | 规模阈值与拆分标准（Claude Code 下由 hook 自动检查） |
| `03-architecture.md` | 设计、分层、重构、写跨平台脚本时 | 架构原则、职责边界、兼容与回退 |
| `04-error-handling.md` | 实现错误处理或异常分支时 | 错误处理与统一错误层规范 |
| `05-git-workflow.md` | 涉及分支、worktree、提交、推送、PR 时 | Worktree 规则、提交格式、授权边界 |
| `06-comments.md` | 写公共 API、复杂逻辑注释时 | 必须注释的场景与格式 |
| `07-forbidden.md` | 编码与提交前的安全检查 | 代码、安全、提交禁止清单 |
| `08-specialty-rules-index.md` | 任务命中专项场景（Hook、测试、性能、编排等）时 | `rules/common/` 专项规则触发矩阵 |
| `09-first-principles-adversarial-testing.md` | 架构调整、重构、质量门等复杂/高风险任务 | 第一性原则与对抗性测试 |
