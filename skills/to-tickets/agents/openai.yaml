interface:
  display_name: "To Tickets"
  short_description: "把 Spec 拆成 tracer-bullet tickets"
  default_prompt: "使用 $to-tickets 将已批准的 Spec 拆成带 blocking edges 的 tickets。"
policy:
  allow_implicit_invocation: false
