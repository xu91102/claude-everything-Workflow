# 学习模式归档

从 Hook 观察、project/global instinct 沉淀的可复用学习产物，按分类保存。

## 目录结构

- `pr/` — PR 工作流相关模式
- `testing/` — 测试策略和 TDD 模式
- `debugging/` — 调试方法和问题排查模式

## 添加流程

1. 使用 `/learn eval --preview` 评估是否值得保存
2. 确认模式可复用后，运行 `/learn eval` 保存
3. 学习产物按分类保存到对应子目录
