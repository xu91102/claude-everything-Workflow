# 错误处理 (CRITICAL)

- 不忽略错误：使用具体异常、上下文、类型区分和统一处理；可用早返回、错误边界、错误转换和日志记录。
- HTTP 错误由拦截器统一处理，业务 API 调用不自行 `try/catch` 并提示通用失败。
- 降级、特殊提示或批量部分失败可以在业务层显式处理。
- 其他异步错误必须用 `try/catch` 或 Promise `catch` 处理，不能吞掉错误。
