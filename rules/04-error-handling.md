# 错误处理政策

- 不吞掉错误或把失败伪装成成功；保留足以定位问题的类型、原因和上下文，同时避免泄露敏感数据。
- 遵循目标项目和框架已有的错误边界、返回类型、日志与重试约定，不强制统一使用拦截器、异常或 `try/catch`。
- 在外部输入、网络、文件系统、数据库和进程边界校验不可信数据；降级、部分成功和重试必须显式且可验证。
- 错误信息应说明失败动作和可采取的下一步；恢复不了时保持原始失败证据并停止扩大副作用。
