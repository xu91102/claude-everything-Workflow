# 代码规模

优先执行目标项目已有的 lint 和规模约束。`hooks/check-code-size.js` 提供兼容性短警告，不代替项目标准；
Markdown 不受该检查。Hook 的配置与关闭方式见 `rules/common/hooks.md`。

没有项目约束时，用函数约 80 行、参数 5 个、嵌套 4 层作为职责审查提示。超限先判断职责和可读性，
不为满足数值拆出透传层，也不机械压缩代码；文件和行长由 Hook 提示，不在规则中重复维护。
