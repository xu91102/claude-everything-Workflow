# 代码规模约束 (CRITICAL)

机械检查由 Hook 强制（`hooks/check-code-size.js`，随编辑触发），模型无需复述数值：

- 单代码文件约 800 行、单测试文件 1000 行、单行 120 字符，超限由 Hook 输出短警告。
- 文档、设计稿、规则说明和 Markdown 不受检查。

需要判断力的部分由模型执行：

- 非 Hook 检查的约束：函数/方法 ≤ 80 行（提取子函数）、函数参数 ≤ 5 个（封装为对象）、嵌套层级 ≤ 4 层（提前返回）。
- 超限警告出现时，评估职责是否应拆分，而不是机械压缩代码。
