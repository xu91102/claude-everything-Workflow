# 安全优先原则

## 必须遵守

| 规则 | 说明 |
| --- | --- |
| 输入验证 | 所有外部输入必须验证 |
| 敏感信息保护 | 禁止硬编码 API keys/tokens/secrets |
| 安全检查 | 不绕过验证 hooks |
| 沙箱隔离 | 危险操作使用沙箱环境 |

## AgentShield 集成

提交前按风险运行安全扫描：

- SQL 注入检测
- XSS 漏洞检测
- 敏感信息泄露检测
- 依赖漏洞扫描
