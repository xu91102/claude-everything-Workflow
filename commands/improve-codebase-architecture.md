---
description: 扫描代码库的 deepening 机会，展示报告并对选中候选做决策访谈
---

# /improve-codebase-architecture - 架构深化审计

读取并执行 `skills/improve-codebase-architecture/SKILL.md`，把 `$ARGUMENTS` 作为扫描范围。

默认只读。任何浏览器展示、文档写入或后续重构都必须分别满足对应授权门。
