---
description: 实现用户选定的获批 ticket
---

# /implement - Ticket Implementation

读取并执行 `skills/implement/SKILL.md`，把 `$ARGUMENTS`、ticket ref、Spec source 和当前项目作为输入。

command 只提供跨宿主入口；`skills/implement/SKILL.md` 是唯一事实来源。
