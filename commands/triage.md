---
description: 对 Issue 或外部 PR 做分类、验证和 agent-ready 状态流转
---

# /triage - 工程请求分诊

读取并执行 `skills/triage/SKILL.md`，把 `$ARGUMENTS` 作为查询或目标 Issue/PR。

默认先只读分析。评论、标签、关闭、写 `.out-of-scope/` 等动作必须展示精确目标并取得确认。
