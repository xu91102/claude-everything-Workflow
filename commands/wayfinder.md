---
description: 为跨多个 agent session 的大型模糊工作维护 decision-ticket map
---

# /wayfinder - 跨 Session 决策地图

读取并执行 `skills/wayfinder/SKILL.md`，把 `$ARGUMENTS` 作为新目标或已有 map 引用。

只规划和解决决策，不直接实现最终交付物。创建或修改 tracker 工件前必须确认目标与写入范围。
