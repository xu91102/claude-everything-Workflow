---
description: 把已批准 Spec 或计划拆成带 blocking edges 的垂直切片 tickets
---

# /to-tickets - Tracer-bullet Tickets

读取并执行 `skills/to-tickets/SKILL.md`，把 `$ARGUMENTS` 作为 Spec、计划、Issue 或当前会话。

在用户批准 ticket 拆分和准确发布目标前，不写本地 tracker、不创建外部 Issue。
