---
description: 压力测试工程决策，并在用户授权后同步领域词汇与 ADR
---

# /grill-with-docs - 带领域文档的决策访谈

读取并组合：

1. `skills/grilling/SKILL.md` 的显式访谈模式；
2. `skills/domain-modeling/SKILL.md` 的 persistent documentation mode。

把 `$ARGUMENTS` 作为目标。逐项解决重大用户决策；术语或满足 ADR 门槛的决策稳定后，
先展示拟写内容和路径，获得授权再更新 `CONTEXT.md`、`CONTEXT-MAP.md` 或 ADR。

完成后返回中央路由，不生成 Spec 或开始实现。
