#!/usr/bin/env node

/**
 * PreCompact Hook: 上下文压缩前保留硬性规则
 * 目的：防止长对话中代码规模约束被遗忘
 */

let input = '';
process.stdin.on('data', chunk => input += chunk);
process.stdin.on('end', () => {
  // PreCompact hook 输入格式:
  // { hook_type: "PreCompact", context: {...} }

  // 输出到 stderr（作为警告，不阻止压缩）
  console.error(`
╔════════════════════════════════════════════════════════════════╗
║                   📌 硬性规则提醒 (CRITICAL)                    ║
╠════════════════════════════════════════════════════════════════╣
║  单文件行数    ≤ 600 行      违反时必须拆分文件                ║
║  单函数/方法   ≤ 80 行       违反时必须提取子函数              ║
║  函数参数      ≤ 5 个        违反时必须封装为对象              ║
║  嵌套层级      ≤ 4 层        违反时必须提前返回或提取函数      ║
║  单行长度      ≤ 120 字符    违反时必须换行                    ║
╠════════════════════════════════════════════════════════════════╣
║  ⚠️  写代码后立即检查，不要等到提交前！                        ║
╚════════════════════════════════════════════════════════════════╝
`);

  // 退出码 0: 继续执行（非阻塞）
  process.exit(0);
});
