# 上下文卫生与 Subagent 边界

上下文是下一轮推理需要的工作集，不是完整聊天、日志或目录归档。

存在能减少关键不确定性的有效路径时，在授权、宿主限制和用户预算内继续调查，不以固定检索轮数终止。重复操作没有新增信息时改变取证方法；证据足够、有效路径耗尽、缺少必要访问权限或达到用户明确预算时停止，并报告证据与缺口。

## 摘要边界

保留目标、已确认决策、关键文件、待办与阻塞、验证结果、风险、关键已否定假设及反证和必须解决的用户问题。删除大段原文、完整日志、目录树、冗长排查过程、无关偏好和没有证据的评价；保留有价值的排除结论，避免压缩后重走死路。

## 委派上下文

是否委派或并行以 `rules/common/agent-orchestration.md` 为唯一来源；本文件只规定进入 Subagent 的上下文边界。

- 输入只包含目标、必要证据、相关路径、已确认决策和输出契约，不复制完整聊天、日志或目录树。
- 默认使用 Fresh；只有缺少完整历史会实质误判时才使用 Fork。

## 路由契约

每个 agent `description` 必须说明触发条件、输入边界、输出契约和禁止动作。Subagent 默认不得执行任何外部 mutation 或不可逆/高风险操作，包括提交、推送、创建或合并 PR、发送消息、写入云文档、修改外部 tracker 或生产状态；例外由 controller 按 `rules/05-git-workflow.md` 处理。

## 回传格式

只返回：一句话结论、可定位证据、风险及触发条件、最小下一步。不得回传大量 grep 输出、完整测试日志或目录树；需要保留时给出文件路径和复现命令。
